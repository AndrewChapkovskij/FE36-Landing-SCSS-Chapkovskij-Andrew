$(document).ready(function () {
  $('.slider__items').slick({
    arrows: true,
    dots: true,
    // adaptiveHeight: true,
    // slidesToShow: 1,
    // slidesToScroll: 1,
    // speed: 300,
    // easing: 'lianer',
    // infinity: true,
    // initialSlide: 0,
    // autoplay: true,
    appendDots: $('#slider'),
    // autoplaySpeed: 3000,
  })
})
